services:
  rabbitmq:
    image: rabbitmq:4.2.4-management-alpine

    container_name: rabbitmq
    ports:
      - '5672:5672'
      - '15672:15672'
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    healthcheck:
      test: ['CMD', 'rabbitmq-diagnostics', '-q', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5

  postgres:
    image: postgres:18.2-alpine3.23
    restart: always
    container_name: air_quality_db
    command: ['postgres', '-c', 'wal_level=logical']
    ports:
      - 5500:5432
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres001}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres001}
      POSTGRES_DB: air_quality
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}']
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - postgres_data:/var/lib/postgresql/data

  data-collector:
    build:
      context: .
      dockerfile: apps/data-collector/Dockerfile
    container_name: data_collector
    ports:
      - '3000:3000'

    env_file:
      - apps/data-collector/.env.example

    environment:
      - RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672
    healthcheck:
      test:
        ['CMD', 'wget', '--spider', '-q', 'http://localhost:3000/api/health']
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 20s
    depends_on:
      rabbitmq:
        condition: service_healthy

  alert-processor:
    build:
      context: .
      dockerfile: apps/alert-processor/Dockerfile
    container_name: alert_processor
    ports:
      - '3001:3001'
    env_file:
      - apps/alert-processor/.env.example

    environment:
      - RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672
      - DATABASE_URL=postgresql://${POSTGRES_USER:-postgres001}:${POSTGRES_PASSWORD:-postgres001}@postgres:5432/air_quality?schema=public

    healthcheck:
      test:
        ['CMD', 'wget', '--spider', '-q', 'http://localhost:3001/api/health']
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 20s
    depends_on:
      rabbitmq:
        condition: service_healthy
      postgres:
        condition: service_healthy

volumes:
  postgres_data:
